# Workflow doc

## Reusable workflows and secrets

Reusable workflows use environement variables, to send them the project has a single repository secret `EXTRA_SECRETS` with all the following variables.

```txt
SENTRY_DSN=xxx
```
